import React from "react";
import axios from "axios";
import { Button, Box } from "rebass";

const Player = params => {
  return (
    <Box>
      <Button>Play</Button>
    </Box>
  );
};

export default Player;
